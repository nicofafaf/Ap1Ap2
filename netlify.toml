# https://docs.netlify.com/configure-builds/file-based-configuration/
[build]
  # Medien-Pfade aus nexusRegistry müssen unter ./assets liegen (siehe npm run check:assets)
  command = "npm run check:assets && npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"
